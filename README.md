### Files & What I Learned Each Day

- **DA_D1.py** → First touch  
  Variables, Data Types, Input/Output, If-Else, Operators (Arithmetic, Comparison, Logical, Bitwise, Membership, Identity), Naming Conventions (camelCase vs snake_case)

- **DA_D2.py** → Lists, Tuples, Sets, Dictionaries + Lots of String Play  
  append(), remove(), pop(), insert(), tuple(), string methods (upper(), lower(), strip(), replace(), split(), find(), count(), startswith(), lstrip(), etc.)

- **DA_D3.py** → Conditionals Deepened + Loops + Time + Turtle + First Look at Pandas  
  if-elif-else statements (nested too!), shorthand if, while loops, for loops, break, continue, pass, nested loops, patterns (stars), time module, turtle graphics, very first look at DataFrames in Pandas

- **DA_D4.py** → Pandas: Filtering & Column Magic  
  selecting columns, .loc and .iloc, multiple conditions, adding Bonus & Final Salary columns, adding new rows, updating cell values, dropping columns & rows

- **DA_D5.py** → Finally Some Charts! 📊  
  matplotlib: line plots, multiple lines &
