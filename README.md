### Files & What I Learned Each Day

- **DA_D1.py** → First touch  
  Variables, data types, input/output, if-else, operators (arithmetic, comparison, logical, bitwise, membership, identity), naming styles (camelCase vs snake_case)

- **DA_D2.py** → Lists, tuples, sets, dicts + lots of string playing  
  append/remove/pop/insert, tuple immutability, string methods (upper/lower/strip/replace/split/find/count/startswith/lstrip etc.)

- **DA_D3.py** → Conditionals deeper + loops + time + turtle + first pandas glimpse  
  if-elif-else (nested too), shorthand if, while/for loops, break/continue/pass, nested loops, patterns (stars), time module, turtle drawing, very first DataFrame

- **DA_D4.py** → Pandas filtering & column magic  
  Selecting columns, .loc / .iloc, multiple conditions, adding Bonus/Final Salary columns, adding rows, updating cells, dropping stuff

- **DA_D5.py** → Finally some charts! 📊  
  Matplotlib basics: line plots, multiple lines + legend, histogram, pie chart, scatter, subplots (bar + scatter side by side)
